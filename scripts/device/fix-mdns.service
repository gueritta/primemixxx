[Unit]
Description=Fix mDNS hostname (primego.local)
After=network.target avahi-daemon.service
Wants=network.target avahi-daemon.service

[Service]
Type=oneshot
ExecStart=/usr/sbin/fix-mdns.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
